[env:hex-master-circuit]
platform = espressif32 @ 6.12.0
board = hex-master-circuit
framework = espidf

; Point to your custom partition file
board_build.partitions = partitions.csv

; Configure flash size for 16MB
board_upload.flash_size = 16MB
board_upload.maximum_size = 16777216 ; 16MB in bytes

; Build flags for LVGL configuration
build_flags = 
    -D LV_CONF_INCLUDE_SIMPLE
    -Wno-missing-field-initializers

; Libraries
lib_deps = 
    lvgl/lvgl@^8.3.11

; Serial Monitor Configuration
monitor_speed = 115200
monitor_filters = 
    esp32_exception_decoder
    time

; Upload Configuration - ESP32-S3 USB-JTAG
upload_protocol = esp-builtin

; Debug Configuration - ESP32-S3 Built-in JTAG
debug_tool = esp-builtin
